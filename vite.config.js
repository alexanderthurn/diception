import { defineConfig } from 'vite'

export default defineConfig({
    // Set base path for production builds
    // Use './' for relative paths (works in any subdirectory)
    // Or use '/2025/dicy/' for absolute subdirectory path
    base: './',
})
